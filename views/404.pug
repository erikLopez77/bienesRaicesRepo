if usuario
    include ./layout/admin
else 
    include ./layout/app
    
block contenido 
    div(class="min-h-screen bg-gradient-to-br from-indigo-50 to-white flex flex-col items-center justify-center py-12 px-4 sm:px-6 lg:px-8")
        div(class="text-center max-w-md mx-auto")
            // Icono de advertencia
            div(class="mx-auto flex items-center justify-center h-16 w-16 rounded-full bg-red-100 mb-4")
                svg(class="h-10 w-10 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor")
                    path(stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z")

            // Título principal
            h2(class="text-3xl font-extrabold text-gray-900 sm:text-4xl mb-3")= pagina || "Página no disponible"
            
            // Mensaje descriptivo
            p(class="mt-3 text-lg text-gray-600") La página a la que deseas acceder no se encuentra disponible o no existe.
            
            // Información adicional
            div(class="mt-6 bg-white shadow rounded-lg p-4")
                p(class="text-gray-700") Puede que el enlace esté roto o la página haya sido movida.
                p(class="mt-2 text-gray-700") Verifica la URL o navega desde nuestro menú principal.

            // Botón para volver al inicio
            div(class="mt-8")
                a(href="/" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500")
                    | Volver al inicio
                    svg(class="ml-2 -mr-1 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24")
                        path(stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6")

    